@model List<GymManagementSystem.Models.Entities.Trainer>
@{
    ViewData["Title"] = "Antrenörlerimiz";
}

<div class="container">
    <div class="text-center mb-5">
        <h1 class="display-4">👨‍🏫 Profesyonel Antrenörlerimiz</h1>
        <p class="lead">Alanında uzman antrenörlerimizle tanışın ve hedeflerinize ulaşın!</p>
    </div>

    <div class="row">
        @foreach (var trainer in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-header bg-success text-white">
                        <h4 class="mb-0">@trainer.FullName</h4>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <p class="mb-1">
                                <strong>📧 Email:</strong> <a href="mailto:@trainer.Email">@trainer.Email</a>
                            </p>
                            @if (!string.IsNullOrEmpty(trainer.PhoneNumber))
                            {
                                <p class="mb-1">
                                    <strong>📞 Telefon:</strong> @trainer.PhoneNumber
                                </p>
                            }
                            <p class="mb-1">
                                <strong>💰 Saatlik Ücret:</strong> @trainer.HourlyRate ₺
                            </p>
                        </div>

                        @if (!string.IsNullOrEmpty(trainer.Bio))
                        {
                            <div class="mb-3">
                                <strong>ℹ️ Hakkında:</strong>
                                <p class="text-muted small">@trainer.Bio</p>
                            </div>
                        }

                        @if (trainer.Specialties.Any())
                        {
                            <div class="mb-3">
                                <strong>🎯 Uzmanlık Alanları:</strong>
                                <div class="mt-2">
                                    @foreach (var specialty in trainer.Specialties)
                                    {
                                        <span class="badge bg-primary me-1 mb-1">@specialty.SpecialtyName</span>
                                    }
                                </div>
                            </div>
                        }

                        @if (trainer.Availabilities.Any())
                        {
                            <div class="mb-3">
                                <strong>📅 Müsaitlik:</strong>
                                <ul class="list-unstyled small mt-2">
                                    @{
                                        var groupedAvailabilities = trainer.Availabilities
                                            .Where(a => a.IsAvailable)
                                            .GroupBy(a => new { a.StartTime, a.EndTime })
                                            .ToList();
                                    }
                                    @foreach (var group in groupedAvailabilities)
                                    {
                                        var days = group.Select(a => GetDayName(a.DayOfWeek)).ToList();
                                        <li>
                                            <span class="badge bg-secondary">@string.Join(", ", days)</span>
                                            @group.Key.StartTime.ToString(@"hh\:mm") - @group.Key.EndTime.ToString(@"hh\:mm")
                                        </li>
                                    }
                                </ul>
                            </div>
                        }
                    </div>
                    <div class="card-footer bg-light">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <a asp-controller="Appointments" asp-action="Create" class="btn btn-success w-100">
                                📅 Randevu Al
                            </a>
                        }
                        else
                        {
                            <a asp-controller="Account" asp-action="Login" class="btn btn-outline-success w-100">
                                Randevu için Giriş Yapın
                            </a>
                        }
                    </div>
                </div>
            </div>
        }
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center">
            <h4>Henüz antrenör eklenmemiş.</h4>
        </div>
    }
</div>

@functions {
    string GetDayName(DayOfWeek day)
    {
        return day switch
        {
            DayOfWeek.Monday => "Pzt",
            DayOfWeek.Tuesday => "Sal",
            DayOfWeek.Wednesday => "Çar",
            DayOfWeek.Thursday => "Per",
            DayOfWeek.Friday => "Cum",
            DayOfWeek.Saturday => "Cmt",
            DayOfWeek.Sunday => "Paz",
            _ => day.ToString()
        };
    }
}
