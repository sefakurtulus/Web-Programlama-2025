@model List<GymManagementSystem.Models.Entities.Trainer>
@{
    ViewData["Title"] = "Antrenör Yönetimi";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>👥 Antrenör Yönetimi</h2>
        <a asp-action="CreateTrainer" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Yeni Antrenör Ekle
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        @foreach (var trainer in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-header bg-info text-white">
                        <h5 class="mb-0">@trainer.FullName</h5>
                    </div>
                    <div class="card-body">
                        <p class="card-text">
                            <strong>📧 Email:</strong> @trainer.Email<br />
                            <strong>📱 Telefon:</strong> @(trainer.PhoneNumber ?? "Yok")<br />
                            <strong>💰 Ücret:</strong> @trainer.HourlyRate ₺/saat
                        </p>

                        @if (trainer.Bio != null)
                        {
                            <p class="text-muted small">@trainer.Bio</p>
                        }

                        @if (trainer.Specialties != null && trainer.Specialties.Any())
                        {
                            <div class="mb-2">
                                <strong>Uzmanlıklar:</strong><br />
                                @foreach (var specialty in trainer.Specialties)
                                {
                                    <span class="badge bg-success me-1">@specialty.SpecialtyName</span>
                                }
                            </div>
                        }

                        @if (trainer.Availabilities != null && trainer.Availabilities.Any())
                        {
                            <div class="mb-2">
                                <strong>Müsaitlik:</strong><br />
                                <small class="text-muted">
                                    @foreach (var avail in trainer.Availabilities.Take(3))
                                    {
                                        <span>@avail.DayOfWeek: @avail.StartTime.ToString(@"hh\:mm") - @avail.EndTime.ToString(@"hh\:mm")</span><br />
                                    }
                                    @if (trainer.Availabilities.Count > 3)
                                    {
                                        <span class="badge bg-secondary">+@(trainer.Availabilities.Count - 3) gün daha</span>
                                    }
                                </small>
                            </div>
                        }
                    </div>
                    <div class="card-footer">
                        <a asp-action="EditTrainer" asp-route-id="@trainer.Id" class="btn btn-sm btn-warning">
                            Düzenle
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center">
            <h4>Henüz antrenör bulunmuyor</h4>
            <p>Yeni antrenör eklemek için yukarıdaki butonu kullanın.</p>
        </div>
    }
</div>
